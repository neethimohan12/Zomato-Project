apiVersion: v1
kind: Service

metadata:
  name: zomato-service

spec:
  type: NodePort

  selector:
    app: swiggy

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
